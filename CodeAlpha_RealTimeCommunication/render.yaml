services:
  - type: web
    name: codealpha-realtimecommunication
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://codealpha-realtimecommunication.netlify.app
